---
const { title, classNames } = Astro.props;
---

<button
  class:list={[
    classNames,
    "lg:px-8 px-4 lg:py-4 py-1 w-fit lg:text-3xl bg-black text-white font-black rounded-full",
  ]}>{title}</button
>
