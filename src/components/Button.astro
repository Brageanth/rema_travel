---
const { title, classNames } = Astro.props;
---

<button
  class:list={[
    classNames,
    "lg:px-8 px-4 lg:py-4 py-1 w-fit lg:text-3xl bg-midnight text-white font-black cursor-pointer rounded-full hover:shadow-xl transition-shadow duration-350 ease-in-out",
  ]}>{title}</button
>
